# Xcode
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# OS
.DS_Store
Thumbs.db

# Secrets
*.env
*.pem
*.p12
*.mobileprovision

# Vendored static libraries are stored in Git LFS (see .gitattributes and
# Vendor/README.md) rather than excluded, so a clone can link without a
# separate out-of-band copy.

build/
